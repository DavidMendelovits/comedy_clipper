# Combined Requirements for All Pose Detection Models
# Install this to use all models (except OpenPose which requires manual model download)

# Common dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# MediaPipe
mediapipe>=0.10.0

# MoveNet (TensorFlow)
tensorflow>=2.13.0
tensorflow-hub>=0.14.0

# MMPose
mmpose>=1.3.0
mmcv>=2.0.0
mmengine>=0.9.0
mmdet>=3.2.0

# PyTorch (for MMPose)
torch>=2.0.0
torchvision>=0.15.0

# YOLO (Ultralytics)
ultralytics>=8.0.0

# Note: For OpenPose, you only need opencv-python but must download models manually
# Note: For GPU support, install CUDA-enabled versions of TensorFlow and PyTorch
